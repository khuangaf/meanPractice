var mainApplicationModuleName = 'mean';

var application = angular.module(mainApplicationModuleName, []);

// angular.element(document)ready(function() {
// 	angular.bootstrap(document,
// 		[mainApplicationModuleName]); 
// });